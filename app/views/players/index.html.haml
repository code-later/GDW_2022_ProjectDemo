%h1 Spieler:innen

#players
  %table
    %thead
      %tr
        %th Name
        %th Nickname
    %tbody
    - @players.each do |player|
      %tr{id: "#{dom_id player}"}
        %th= link_to player.name, player
        %th= player.nickname

= link_to new_player_path do
  %button Neue Spieler:in
